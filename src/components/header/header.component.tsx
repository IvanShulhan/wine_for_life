import { Grid, Typography } from '@mui/material';

export const HeaderComponent = () => {
  return (
    <Grid container>
      <Grid item xs={2}>
        <Typography>Logo</Typography>
      </Grid>
      <Grid item xs={10}>
      <Typography>Logo</Typography>
      </Grid>
    </Grid>
  )
}